import React from "react";
import styled from "styled-components";

const Container = styled.div`
  color: black;
`;

const Show = ({}) => {
  //console.log(props);
  return (
    <Container>
      <h2>Acá estoy!</h2>
    </Container>
  );
};

export default Show;
